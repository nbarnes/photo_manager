
<div class='photo_title'>
  <b><%= photo.title %></b>
</div>

<div>
  <% if photo.image != "" %>
    <%= image_tag photo.image, size: '384x240', class: 'photo_image' %>
  <% end %>
</div>

<div class='photo_tags'><b>Tags:</b>
  <%= photo.tag_list %>
</div>

<div>
  <b>Member of gallaries:</b>

  <% photo.gallaries.each do |gallary| %>
    <%= gallary.name %>
  <% end %>
</div>